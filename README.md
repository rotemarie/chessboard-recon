# chessboard-recon
Given real chessboard images, the goal is to classify each of the 64 board squares into one of the possible piece classes (white pawn, black knight, etc.). The system should then reconstruct the chess diagram (based on the FEN notation) based on the per-square predictions.
